/**
 * AloHost Theme Controller
 * Page stays in light mode (Airbnb default).
 * No floating toggle — the preview bar handles
 * mockup dark/light switching independently.
 */

(function() {
  document.documentElement.setAttribute('data-theme', 'light');
})();
